package dev.s7a.strata.integration.external

/**
 * Typed node identities used by the external lifecycle probe.
 */
public enum class ExternalNodeId {
    /**
     * The external root node.
     */
    Root,

    /**
     * The external child node.
     */
    Child,
}
