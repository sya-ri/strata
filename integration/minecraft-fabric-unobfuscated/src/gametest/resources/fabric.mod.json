{
  "schemaVersion": 1,
  "id": "${integrationModId}",
  "version": "${version}",
  "name": "Strata Minecraft ${minecraftVersion} integration verification",
  "environment": "client",
  "mixins": ${gameTestMixins},
  "entrypoints": {
    "fabric-client-gametest": [
      "${gameTestEntrypoint}"
    ]
  },
  "depends": {
    "${runtimeModId}": "*"
  }
}
